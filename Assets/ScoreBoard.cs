using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.UI;

public class ScoreBoard : MonoBehaviour
{

    /*

    public Text highScore;
    //public static int score = 0;
    //public Text scoreString;

    //public Transform shipTransf;
    //private float timeSpeed;
    // Use this for initialization
    void Start()
    {
        highScore.text = "Highscore: " + PlayerPrefs.GetInt("Highscore", 0).ToString();
    }


    void setScore()
    {

    }



    // Update is called once per frame
    /*
    void Update()
    {
       
        // float shipTrf = shipTransf.position.z;
       // if (shipTrf < 240.0f)
       // {
       //     shipTrf = 240.0f;
       // }

        //32.0f é o valor combinado com 0.03125f (valor redutor no score) para que o resultado vire 1
        float timee = Time.time;
        //if (timee < 32.0f)
        //{
        //    timee = 32.0f;
        //}
        //else
        //{
        //    timee = Time.time;
        //}

        //score += ((Mathf.RoundToInt(timee)));
        score = Mathf.RoundToInt(shipTransf.position.z) + OlusGetShot.addScore;

        scoreString.text = score.ToString();

        if (score > PlayerPrefs.GetInt("Highscore",0))
        {
            PlayerPrefs.SetInt("Highscore", score);
            highScore.text = "Highscore: " + score.ToString();
        }

       
}
 */

    //void OnGUI()
    //{
    //    GUI.Box(new Rect(100,100,100,100), score.ToString());
    //}



    
}